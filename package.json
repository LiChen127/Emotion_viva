{
  "name": "emotion-viva",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "pnpm run -r dev",
    "dev:api": "pnpm --filter @emotion-viva/api dev",
    "dev:scraper": "pnpm --filter @emotion-viva/scraper dev",
    "build": "pnpm run -r build",
    "build:libs": "pnpm run --filter \"@emotion-viva/core\" build && pnpm run --filter \"@emotion-viva/common\" build",
    "build:apps": "pnpm run --filter \"@emotion-viva/api\" build && pnpm run --filter \"@emotion-viva/scraper\" build",
    "test": "pnpm run -r test",
    "clean": "pnpm run -r clean",
    "lint": "pnpm run -r lint"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
